import { generateKeyPair as generateKeyPair$1 } from '../runtime/generate.mjs';

async function generateKeyPair(alg, options) {
    return generateKeyPair$1(alg, options);
}

export default generateKeyPair;
export { generateKeyPair };
