import { generateSecret as generateSecret$1 } from '../runtime/generate.mjs';

async function generateSecret(alg, options) {
    return generateSecret$1(alg);
}

export default generateSecret;
export { generateSecret };
