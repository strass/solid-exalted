import { KeyPair } from "@inrupt/solid-client-authn-core";
export declare function buildBearerFetch(authToken: string): typeof fetch;
export declare function buildDpopFetch(authToken: string, dpopKey: KeyPair): Promise<typeof fetch>;
